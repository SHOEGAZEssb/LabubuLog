@page "{id:int}/rate"
@model LabubuLog.Pages.Games.RateModel
@{
    ViewData["Title"] = "Rate game";
}

<section class="page-heading">
    <div>
        <p class="eyebrow">Ratings</p>
        <h1>@Model.Game.Title</h1>
    </div>
</section>

<form method="post" class="edit-surface compact-form">
    <div asp-validation-summary="ModelOnly" class="text-danger"></div>
    <div class="form-grid">
        <div class="form-floating">
            <input class="form-control" asp-for="Input.YourRating" placeholder="Lebubu Score" />
            <label asp-for="Input.YourRating"></label>
            <span class="text-danger" asp-validation-for="Input.YourRating"></span>
        </div>
        <div class="form-floating">
            <input class="form-control" asp-for="Input.PartnerRating" placeholder="Labubu Score" />
            <label asp-for="Input.PartnerRating"></label>
            <span class="text-danger" asp-validation-for="Input.PartnerRating"></span>
        </div>
        <div class="form-floating wide">
            <textarea class="form-control tall-input" asp-for="Input.FavoriteMoment" placeholder="Favorite moment"></textarea>
            <label asp-for="Input.FavoriteMoment"></label>
            <span class="text-danger" asp-validation-for="Input.FavoriteMoment"></span>
        </div>
        <div class="form-floating wide">
            <textarea class="form-control tall-input" asp-for="Input.RatingNotes" placeholder="Rating notes"></textarea>
            <label asp-for="Input.RatingNotes"></label>
            <span class="text-danger" asp-validation-for="Input.RatingNotes"></span>
        </div>
    </div>
    <div class="form-actions">
        <button class="btn btn-primary" type="submit">Save scores</button>
        <a class="btn btn-outline-secondary" asp-page="Details" asp-route-id="@Model.Game.Id">Cancel</a>
    </div>
</form>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
